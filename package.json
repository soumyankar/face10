{
  "name": "system",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd server/ && node server.js\" \"cd client/ && npm start\"",
    "installall": "concurrently \"cd client/ && npm install\" \"cd server/ && npm install\"",
    "build": "cd client && npm run build",
    "heroku-postbuild": "npm run installall",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}
